SKOLAR ADMINISTRATOR LOGIN
==========================

Run the project:
  dotnet restore
  dotnet run

The application applies existing database migrations automatically when it starts.

Open:
  /Home/Account?role=admin

Email:
  skolartutors.ph@gmail.com

Password:
  admin_132

The administrator account is created and synchronized automatically when the application starts.
A future email-verification update can send the one-time code only to this fixed administrator email.
